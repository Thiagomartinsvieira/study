// Escreva uma função chamada paisagem que recede dois argumentos, largura e altura de uma imagem (number).
// Retorne true se a imagem estiver no modo paisagem. 

// function paisagem(largura, altura){
//         return largura > altura;
// }

const paisagem = (largura, altura) => largura > altura;

console.log(paisagem(10000, 1120));